{
  "name": "relay-functions",
  "description": "Server-side rate limiting and moderation support for Relay. Deploy with: cd functions && npm install && firebase deploy --only functions",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  },
  "private": true
}
